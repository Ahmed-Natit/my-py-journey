big = max('hello world')  # returns the highest character in terms of Unicode value (alphabetical order here)
print(big)


tiny = min('hello world') # returns the character with the lowest Unicode value (here it's the space; alphabetically it'd be 'd')
print(tiny)
